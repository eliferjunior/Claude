# Porta do servidor (padrão: 3000)
PORT=3000

# Segredo para assinatura de sessões (OBRIGATÓRIO alterar em produção)
SESSION_SECRET=change-this-to-a-random-secret

# Ambiente (production ou development)
NODE_ENV=production
